<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mybitchinapps.cortana.daemonslayer.discover_delhi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/DiscoverTheme">
        <activity android:name="mybitchinapps.cortana.daemonslayer.discover_delhi.activity.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="mybitchinapps.cortana.daemonslayer.discover_delhi.activity.MainActivity"
            android:label="@string/title_activity_welcome"
            android:theme="@style/DiscoverTheme">
        </activity>
    </application>

</manifest>